import { Kanit, Sarabun } from "next/font/google";

export const kanit = Kanit({
  weight: ["400", "700"],
  subsets: ["thai"],
  display: "swap",
});

export const sarabun = Sarabun({
  weight: ["400", "700"],
  subsets: ["thai"],
  display: "swap",
});
